const Footer = () => {
    return (
        <footer className={'flex justify-center'}>
            <div>
                Copyright © {new Date().getFullYear()}
            </div>
        </footer>
    );
}

export default Footer;